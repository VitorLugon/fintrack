# Server

Esta pasta receberá autenticação, autorização, serviços e queries Prisma.

Toda operação com dados financeiros deverá obter o usuário autenticado no
servidor e filtrar os registros por `userId`.
